import { PlanetaryDataRecord } from './planetary-data-record';

describe('PlanetaryDataRecord', () => {
  it('should create an instance', () => {
    expect(new PlanetaryDataRecord()).toBeTruthy();
  });
});
