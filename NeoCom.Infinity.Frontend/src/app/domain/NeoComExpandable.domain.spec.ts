import { NeoComExpandable } from './NeoComExpandable.domain';

describe('NeoComExpandable', () => {
    it('should create an instance', () => {
        expect(new NeoComExpandable()).toBeTruthy();
    });
});
